$(function() {

	$("li:contains('pine')").text("almonds");

	//$("li.hot").html("<em></em>");

	$("li#one").remove();
});
 //use append for adding new content inclass
 // add a prepend and a + , do not need to doa  for loop
